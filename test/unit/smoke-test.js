"use strict";

var request = require('supertest'),
    should = require('should');

describe('thing-server', () => {

    before(done => {
        done();
    });

    after(done => {
        // Call after all tests
        done();
    });

    beforeEach(done => {
        // Call before each test
        done();
    });

    afterEach(done => {
        // Call after each test
        done();
    });

    it('shoud do something', done => {
        done();
    });

});
