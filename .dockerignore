node_modules
npm-debug.log

# Local build output — the builder stage compiles its own dist/
dist
coverage

# Test / CI / dev files not needed in the runtime image
features
test
cucumber.json
.github
.git
